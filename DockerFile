# 1. Using an official lightweight Python image
FROM python:3.11-slim

# 2. Set the working directory inside the container
WORKDIR /app

# 3. Set environment variables
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONUNBUFFERED 1

# 4. Install system dependencies (good practice)
RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# 5. Copy and install Python requirements
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 6. Copy the entire application code into the container
COPY . .

# 7. Expose the port the app will run on
# Google Cloud Run expects 8080 by default
EXPOSE 8080

# 8. The command to run the application
# We run 'main:app' (the 'app' object in 'main.py')
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8080"]